{"ArtifactMetadata": "{\"ArtifactType\":\"other\",\"RequestFeedback\":false,\"Summary\":\"Robust integration script using bracket-depth parsing and comments skipping to parse all lessons 3 to 12 from data.js, structure them into standard exercises, update Unit 40 rawTopics, and save data.js.\"}", "CodeContent": "\"const fs = require('fs');\\nconst path = require('path');\\n\\nconst dataFilePath = path.join(__dirname, '../data.js');\\nlet dataContent = fs.readFileSync(dataFilePath, 'utf8');\\n\\n// Find where the IIFE ends (the last line of the clean codebase)\\nconst iifeEndStr = '})();';\\nconst iifeEndIndex = dataContent.indexOf(iifeEndStr);\\n\\nif (iifeEndIndex === -1) {\\n  console.error(\\\"Could not find })(); marker in data.js!\\\");\\n  process.exit(1);\\n}\\n\\n// Split the file into clean base code and trailing data\\nconst cleanBaseCode = dataContent.substring(0, iifeEndIndex + iifeEndStr.length).trim() + '\\\\n';\\nconst trailingData = dataContent.substring(iifeEndIndex + iifeEndStr.length).trim();\\n\\nconsole.log(\\\"Clean base code length:\\\", cleanBaseCode.length);\\nconsole.log(\\\"Trailing data length:\\\", trailingData.length);\\n\\nconst lessonsMap = {};\\n\\n// Parse trailingData using a brace/bracket matching scanner\\nlet depth = 0;\\nlet start = -1;\\nlet inString = false;\\nlet stringChar = null;\\nlet escape = false;\\n\\nfor (let i = 0; i < trailingData.length; i++) {\\n  const char = trailingData[i];\\n  \\n  if (escape) {\\n    escape = false;\\n    continue;\\n  }\\n  \\n  if (char === '\\\\\\\\') {\\n    escape = true;\\n    continue;\\n  }\\n  \\n  if (inString) {\\n    if (char === stringChar) {\\n      inString = false;\\n    }\\n    continue;\\n  }\\n  \\n  if (char === '\\\"' || char === \\\"'\\\") {\\n    inString = true;\\n    stringChar = char;\\n    continue;\\n  }\\n  \\n  // Skip comments\\n  if (char === '/' && trailingData[i + 1] === '/') {\\n    while (i < trailingData.length && trailingData[i] !== '\\\\n') {\\n      i++;\\n    }\\n    continue;\\n  }\\n  if (char === '/' && trailingData[i + 1] === '*') {\\n    i += 2;\\n    while (i < trailingData.length && !(trailingData[i] === '*' && trailingData[i + 1] === '/')) {\\n      i++;\\n    }\\n    i++;\\n    continue;\\n  }\\n  \\n  if (char === '{' || char === '[') {\\n    if (depth === 0) {\\n      start = i;\\n    }\\n    depth++;\\n  } else if (char === '}' || char === ']') {\\n    depth--;\\n    if (depth === 0 && start !== -1)\n<truncated 17415 bytes>", "Description": "\"Write robust integration script.\"", "IsArtifact": "false", "Overwrite": "true", "TargetFile": "\"/Users/faruknafizfazlioglu/Desktop/amok/scratch/integrate_all_u40_lessons.js\"", "toolAction": "\"Updating integrate_all_u40_lessons.js script with robust parser\"", "toolSummary": "\"File edit\""}
{"ArtifactMetadata": "{\"ArtifactType\":\"other\",\"RequestFeedback\":false,\"Summary\":\"Robust integration script using bracket-depth parsing and comments skipping to parse all lessons 3 to 12 from data.js, structure them into standard exercises, update Unit 40 rawTopics, and save data.js.\"}", "CodeContent": "\"const fs = require('fs');\\nconst path = require('path');\\n\\nconst dataFilePath = path.join(__dirname, '../data.js');\\nlet dataContent = fs.readFileSync(dataFilePath, 'utf8');\\n\\n// Find where the IIFE ends (the last line of the clean codebase)\\nconst iifeEndStr = '})();';\\nconst iifeEndIndex = dataContent.indexOf(iifeEndStr);\\n\\nif (iifeEndIndex === -1) {\\n  console.error(\\\"Could not find })(); marker in data.js!\\\");\\n  process.exit(1);\\n}\\n\\n// Split the file into clean base code and trailing data\\nconst cleanBaseCode = dataContent.substring(0, iifeEndIndex + iifeEndStr.length).trim() + '\\\\n';\\nconst trailingData = dataContent.substring(iifeEndIndex + iifeEndStr.length).trim();\\n\\nconsole.log(\\\"Clean base code length:\\\", cleanBaseCode.length);\\nconsole.log(\\\"Trailing data length:\\\", trailingData.length);\\n\\nconst lessonsMap = {};\\n\\n// Parse trailingData using a brace/bracket matching scanner\\nlet depth = 0;\\nlet start = -1;\\nlet inString = false;\\nlet stringChar = null;\\nlet escape = false;\\n\\nfor (let i = 0; i < trailingData.length; i++) {\\n  const char = trailingData[i];\\n  \\n  if (escape) {\\n    escape = false;\\n    continue;\\n  }\\n  \\n  if (char === '\\\\\\\\') {\\n    escape = true;\\n    continue;\\n  }\\n  \\n  if (inString) {\\n    if (char === stringChar) {\\n      inString = false;\\n    }\\n    continue;\\n  }\\n  \\n  if (char === '\\\"' || char === \\\"'\\\") {\\n    inString = true;\\n    stringChar = char;\\n    continue;\\n  }\\n  \\n  // Skip comments\\n  if (char === '/' && trailingData[i + 1] === '/') {\\n    while (i < trailingData.length && trailingData[i] !== '\\\\n') {\\n      i++;\\n    }\\n    continue;\\n  }\\n  if (char === '/' && trailingData[i + 1] === '*') {\\n    i += 2;\\n    while (i < trailingData.length && !(trailingData[i] === '*' && trailingData[i + 1] === '/')) {\\n      i++;\\n    }\\n    i++;\\n    continue;\\n  }\\n  \\n  if (char === '{' || char === '[') {\\n    if (depth === 0) {\\n      start = i;\\n    }\\n    depth++;\\n  } else if (char === '}' || char === ']') {\\n    depth--;\\n    if (depth === 0 && start !== -1)\n<truncated 17415 bytes>", "Description": "\"Write robust integration script.\"", "IsArtifact": "false", "Overwrite": "true", "TargetFile": "\"/Users/faruknafizfazlioglu/Desktop/amok/scratch/integrate_all_u40_lessons.js\"", "toolAction": "\"Updating integrate_all_u40_lessons.js script with robust parser\"", "toolSummary": "\"File edit\""}
